
  <%= form_with(model: account) do |form| %>
    <% if account.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

        <ul>
          <% account.errors.each do |error| %>
           <li><%= error.full_message %></li>
          <% end %>
       </ul>
      </div>
    <% end %>

    <div>
      <%= form.label :first_name,class: "text-white", style: "display: block" %>
      <%= form.text_field :first_name , class: "form-control" %>
    </div>

    <div>
      <%= form.label :last_name,class: "text-white", style: "display: block" %>
      <%= form.text_field :last_name, class: "form-control" %>
    </div>

    <div>
      <%= form.label :balance,class: "text-white", style: "display: block" %>
      <%= form.text_field :balance, class: "form-control" %>
    </div>

    <div>
      <%= form.label :email,class: "text-white", style: "display: block" %>
      <%= form.text_field :email, class: "form-control" %>
    </div>

    <div>
      <br>
      <%= form.submit class: "btn btn-primary" %>
    </div>
  <% end %>